import { FibonacciEstimation } from './fibonacci-estimation';

describe('FibonacciEstimation', () => {
  it('should be defined', () => {
    expect(new FibonacciEstimation('toto')).toBeDefined();
  });
});
